<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    
    <PropertyGroup>
        <MTP_IsEnabled Condition="$(DesignTimeBuild) != 'true'">true</MTP_IsEnabled>
    </PropertyGroup>
    
</Project>